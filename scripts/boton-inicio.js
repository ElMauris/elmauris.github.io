btnIniciar.addEventListener('click', function () {

    // 3. Cuando ocurre el clic, le removemos la clase 'oculto' a la sección de carreras
    btnCarreras.classList.remove('oculto');

    btnIniciar.classList.add('oculto')
});
